useDynLib(Eagle)
importFrom(parallel, detectCores)
importFrom(data.table, fread)
importFrom(matrixcalc, is.positive.definite)
importFrom(shinyjs, useShinyjs)
import(shinyFiles, shiny,  shinythemes, shinyBS, stats, utils )

export(
   ReadPheno,
   ReadMarker,
   ReadMap,
   ReadZmat,
   AM,
   OpenGUI,
   SummaryAM)
