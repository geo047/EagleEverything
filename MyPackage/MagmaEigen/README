This directory contains the  Magma cpp code for performing eigendecomposition for symmetric matrices. 

To build the executable, run make.bat

The executable is copied into the Eagle package via the  ../build_INSTALL_package.bsh


