to clone this directory use

git clone git@github.com:geo047/EagleEverything.git  

(this needs a ssh key to be available and the public key put on 
github/settings/ssh keys/add new key etc. ... 


git push -u origin   master

username: geo047
passwd: old password


Pushing sequence

git add -A   ## adds all changes to local repository
git commit -m "add your own comment"
git push

## this will not ask for a username or password ---


